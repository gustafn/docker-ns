# examples/openacs-org/.env.example
#
# Copy to .env and adjust paths/addresses for your host:
#   cp .env.example .env

# --- required for this stack ---
hostname=example.org
oacs_serverroot=/var/www/example.org

# Use a separate log directory. This is important, when multiple
# instances should run on the same server, since the file system is
# outside of the container.
logdir=/var/www/example.org/log-container

# --- site-specific NaviServer config ---
# Tailored config file (multi-server / multi-domain setups are typical here)
nsdconfig=/var/www/example.org/etc/openacs-config-example.tcl

# --- TLS certificate (shared by OpenACS + mail-relay) ---
# Non-standard location is supported; defaults to ${oacs_serverroot}/etc/certfile.pem
certificate=/var/www/example.org/etc/example.org.pem

# --- public bindings (dual-stack) ---
# Use explicit addresses for production; keep loopback for local testing.
ipaddress=127.0.0.1
ipv6address=::1
httpport=80
httpsport=443

# --- database access (domain socket mounted into containers) ---
# db_port is also part of the socket filename: /tmp/.s.PGSQL.<db_port>
# since we are using here "localhost" as db_host
db_user=openacs
db_host=localhost
db_port=5432

# --- internal loopback server used for healthchecks / internal access ---
internal_loopbackport=8888

# --- optional extra packages (runtime) ---
system_pkgs=imagemagick

# --- optional memory optimization (tcmalloc) ---
# Example path on Debian/Ubuntu amd64:
# LD_PRELOAD=/usr/lib/x86_64-linux-gnu/libtcmalloc_minimal.so.4
LD_PRELOAD=
